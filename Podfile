# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GeoHashingOnMapKit' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GeoHashingOnMapKit
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'GeoFire', '~> 3.0'
  target 'GeoHashingOnMapKitTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GeoHashingOnMapKitUITests' do
    # Pods for testing
  end

end
